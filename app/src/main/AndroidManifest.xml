<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.admin2.payments">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.NFC" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".RegisterPage" />

        <activity android:name=".UserArea">

        </activity>

        <activity android:name=".SendMoneyPage" />
        <activity android:name=".ReceiveMoneyPage" />
        <activity android:name=".Enter" />
        <activity android:name=".Transactions.UserArea2"></activity>
        <activity android:name=".Transactions.LoginActivity2" />



    </application>

</manifest>